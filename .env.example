# =======================================================
# ROOT ENVIRONMENT CONFIGURATION
# Premium Hair Wigs & Extensions E-commerce Platform
# =======================================================
# This file contains all environment variables needed across
# all three components: Backend API, Frontend, and Admin Dashboard
# Copy this file to .env and update with your actual values
# =======================================================

# =======================================================
# BACKEND API CONFIGURATION
# =======================================================

# Server Configuration
NODE_ENV=development
PORT=3000

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=premium_hair_ecommerce
DB_USER=postgres
DB_PASSWORD=your_secure_password_here

# Alternative: Use DATABASE_URL instead of individual DB_ variables
# DATABASE_URL=postgresql://user:password@host:port/database

# JWT Authentication
JWT_SECRET=your_jwt_secret_key_change_this_in_production_min_32_chars
JWT_EXPIRES_IN=24h

# Admin Account (Default credentials - CHANGE on first login!)
ADMIN_EMAIL=admin@premiumhairsa.co.za
ADMIN_PASSWORD=ChangeThisPassword123!

# Security Settings
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
LOGIN_RATE_LIMIT_MAX=5
BCRYPT_ROUNDS=12

# CORS Configuration (URLs that can access the API)
FRONTEND_URL=http://localhost:8000
ADMIN_URL=http://localhost:8001

# =======================================================
# BUSINESS CONFIGURATION
# =======================================================

COMPANY_NAME=Premium Hair Wigs & Extensions Pty (Ltd)
SUPPORT_EMAIL=support@premiumhairsa.co.za
VAT_RATE=0.15

# =======================================================
# PAYMENT GATEWAY - PAYFAST (South Africa)
# =======================================================

# For testing, use sandbox credentials
# For production, use your actual PayFast merchant credentials
PAYFAST_MERCHANT_ID=10000100
PAYFAST_MERCHANT_KEY=46f0cd694581a
PAYFAST_PASSPHRASE=your_secure_passphrase_here
PAYFAST_URL=https://sandbox.payfast.co.za/eng/process

# Production PayFast URL:
# PAYFAST_URL=https://www.payfast.co.za/eng/process

# =======================================================
# EMAIL CONFIGURATION (Optional)
# =======================================================
# Configure if you want to send emails for order confirmations, etc.

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_email_password

# =======================================================
# FRONTEND CONFIGURATION
# =======================================================
# These are used by the frontend application

# API Base URL (where the backend is hosted)
VITE_API_BASE_URL=http://localhost:3000/api

# =======================================================
# ADMIN DASHBOARD CONFIGURATION
# =======================================================

# Admin Dashboard Port (if running separately)
ADMIN_PORT=8001

# API Base URL for admin dashboard
ADMIN_API_BASE_URL=http://localhost:3000/api

# =======================================================
# PRODUCTION DEPLOYMENT SETTINGS
# =======================================================
# Update these when deploying to production (Render, Vercel, etc.)

# Production Database (Render PostgreSQL)
# DATABASE_URL=postgresql://user:pass@host.render.com/database

# Production URLs
# FRONTEND_URL=https://your-username.github.io/ecommerce-frontend
# ADMIN_URL=https://premium-hair-admin.onrender.com
# BACKEND_URL=https://premium-hair-backend.onrender.com

# =======================================================
# OPTIONAL: ANALYTICS & MONITORING
# =======================================================

# Google Analytics (if using)
# GA_TRACKING_ID=UA-XXXXXXXXX-X

# Sentry Error Tracking (if using)
# SENTRY_DSN=your_sentry_dsn

# =======================================================
# OPTIONAL: FILE STORAGE
# =======================================================
# If using cloud storage for product images

# AWS S3
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key
# AWS_BUCKET_NAME=your_bucket_name
# AWS_REGION=us-east-1

# Cloudinary
# CLOUDINARY_CLOUD_NAME=your_cloud_name
# CLOUDINARY_API_KEY=your_api_key
# CLOUDINARY_API_SECRET=your_api_secret

# =======================================================
# NOTES
# =======================================================
# 1. Never commit the actual .env file to version control
# 2. Generate strong random strings for JWT_SECRET (min 32 characters)
# 3. Change all default passwords immediately after first login
# 4. Use environment-specific values (development vs production)
# 5. Keep credentials secure and never share them publicly
